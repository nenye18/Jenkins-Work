pipeline {
    agent any
    tools {
        maven 'maven-3.9'
    }
    stages {
        stage('build') {
            steps {
                echo 'building the application..'
                //sh 'npm install'
                //sh 'npm build'
            }
        }
        stage('build image') {
            steps {
                echo 'building the application..'
                //sh 'npm install'
                //sh 'npm build'
            }
        }
      stage('test') {
            steps {
                echo 'Hello, testing application'
                //sh 'npm test'
            }
        }
    stage('deploy') {
            steps {
                echo 'Hello, deploying application'
            }
        }
   
}
